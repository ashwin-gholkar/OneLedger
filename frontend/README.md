# OneLedger Frontend

This folder is reserved for the React UI application.

Recommended setup:

- React with Vite

Keep frontend source, UI assets, and frontend dependencies inside this folder.

Create the app:

```bash
npm create vite@latest . -- --template react
npm install
npm run dev
```
